rollup

Install + plugin Terse

https://rollupjs.org/tutorial/#installing-rollup-locally


NOTES:

Create a file in the project root called rollup.config.mjs, and add the following code:
// rollup.config.mjs
export default {
	input: 'src/main.js',
	output: {
		file: 'bundle.js',
		format: 'iife'
	}
};

iife – A self-executing function

PLUG-IN Terse

npm install --save-dev @rollup/plugin-terser

// rollup.config.mjs
import terser from '@rollup/plugin-terser';
export default {
	input: 'src/main.js',
	output: [
		{
			file: 'bundle.js',
			format: 'iife'
		},
		{
			file: 'bundle.min.js',
			format: 'iife',
			name: 'version',
			plugins: [terser({
                          compress: false,
                          module: true
                        })]
		}
	]
};